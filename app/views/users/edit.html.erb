<h1>Edit User</h1>

<%= render 'form' %>

<div class="well form-group col-sm-5 col-sm-offset-2">
  <%= link_to 'Delete', @user, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger col-sm-offset-4 col-sm-4" %>
</div>
